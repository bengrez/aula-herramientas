-- Deliberately empty public example.
--
-- Never commit enrollment codes, their hashes, participant references or
-- enrollment UUIDs. Provision the private backend from a restricted local
-- source controlled by the teacher. The ignored production filename is:
--
--   supabase/seed-enrollment-hashes.sql

begin;

commit;
